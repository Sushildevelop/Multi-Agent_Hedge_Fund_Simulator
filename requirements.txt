# Python dependencies placeholder
# =============================================================================
# Multi-Agent Stock Investment Advisor — requirements.txt
# Target runtime: Python 3.13.x
# Verified against PyPI as of July 2026. See README-requirements.md for the
# full compatibility notes, conflict list, and installation order.
# Versions marked "(verify)" below moved fast enough during research that you
# should confirm the exact latest patch with `pip index versions <pkg>` or
# `uv add <pkg>` before locking for production — see Section 10.
# =============================================================================

# --- Web Framework ---------------------------------------------------------
fastapi[standard]==0.136.1
uvicorn[standard]==0.35.0
starlette==0.48.0
python-multipart==0.0.20

# --- Agent Framework / RAG ---------------------------------------------------
langchain==1.6.2
langchain-core==1.4.9
langchain-text-splitters==1.1.2
langchain-community==0.4.2
langchain-openai==1.3.5
langgraph==1.2.9
langgraph-checkpoint==2.1.1
langgraph-sdk==0.2.10
langsmith==0.8.18

# --- LLM Providers ------------------------------------------------------------
groq==0.31.0
openai==1.99.0

# --- Embeddings -----------------------------------------------------------
langchain-huggingface==0.3.1
sentence-transformers==4.1.0
transformers==4.55.0
tokenizers==0.21.4
huggingface-hub==0.34.0

# --- Torch (CPU build shown; swap index-url for CUDA — see README) --------
torch==2.13.0

# --- Vector Database --------------------------------------------------------
qdrant-client==1.18.0
langchain-qdrant==0.3.1

# --- Database (MongoDB Atlas) ------------------------------------------------
beanie==2.1.0
motor==3.7.1
pymongo==4.13.2

# --- Queue / Caching ----------------------------------------------------------
redis==5.3.1
hiredis==3.1.0
celery==5.5.2

# --- Financial APIs -----------------------------------------------------------
yfinance==0.2.65
alpha_vantage==3.0.0
newsapi-python==0.2.7

# --- Document Processing -----------------------------------------------------
pymupdf==1.26.1
unstructured[pdf,docx]==0.18.11
pypdf==5.6.1
python-docx==1.2.0

# --- Data / Scientific --------------------------------------------------------
pandas==2.3.1
numpy==2.3.1
scikit-learn==1.7.0

# --- Config & Validation --------------------------------------------------------
pydantic==2.12.0
pydantic-settings==2.9.1
python-dotenv==1.1.0

# --- Auth & Security -----------------------------------------------------------
pyjwt[crypto]==2.10.1
passlib[bcrypt]==1.7.4
bcrypt==4.3.0

# --- HTTP / Async Clients --------------------------------------------------------
httpx==0.28.1
aiohttp==3.12.13

# --- Serialization & Resilience --------------------------------------------------------
orjson==3.10.18
tenacity==9.1.2
tiktoken==0.9.0

# --- Logging --------------------------------------------------------------------
structlog==25.4.0
loguru==0.7.3

# --- Observability (OpenTelemetry) -----------------------------------------------------
opentelemetry-api==1.36.0
opentelemetry-sdk==1.36.0
opentelemetry-instrumentation-fastapi==0.57b0
opentelemetry-exporter-otlp==1.36.0
prometheus-client==0.22.1